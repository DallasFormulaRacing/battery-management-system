{
  "branches": ["master", "semver"],
  "plugins": [
    "@semantic-release/commit-analyzer",
    "@semantic-release/release-notes-generator",
    "@semantic-release/changelog",
    [
      "@semantic-release/exec",
      {
        "prepareCmd": "echo '#ifndef VERSION_H\\n#define VERSION_H\\n\\n#define FW_VERSION \"${nextRelease.version}\"\\n#define FW_VERSION_MAJOR ${nextRelease.version.split(\".\")[0]}\\n#define FW_VERSION_MINOR ${nextRelease.version.split(\".\")[1]}\\n#define FW_VERSION_PATCH ${nextRelease.version.split(\".\")[2]}\\n\\n#endif' > bms/Core/Inc/version.h"
      }
    ],
    [
      "@semantic-release/git",
      {
        "assets": ["bms/Core/Inc/version.h", "CHANGELOG.md"],
        "message": "chore(release): ${nextRelease.version} [skip ci]"
      }
    ],
    "@semantic-release/github"
  ]
}
